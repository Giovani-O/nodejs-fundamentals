// Buffer é uma representação de um espaço na memória do computador
// É uma maneira de ler e escrever na memória de forma muito performática

const buff = Buffer.from('Hello World')

console.log(buff.toJSON())
